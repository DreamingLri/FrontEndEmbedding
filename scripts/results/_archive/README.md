# 结果归档区说明

这个目录用于集中存放：

1. 旧版 smoke 结果
2. 已退出默认口径的历史结果
3. 仅供回溯的调参结果

当前应优先保留在活跃区的结果文件，只包括：

1. `main_106` 主结果
2. `holdout_v3` 主结果
3. `platform_mixed_daily_v1_2_reviewed` 主结果
4. 当前仍在使用的 `route_v2` / `kb_absent_v2` 边界回归结果
